# cnss-daemon
vendor_internal_prop(cnss_prop);

# Fingerprint
vendor_public_prop(vendor_fp_prop);

# Mlipay
vendor_public_prop(mlipay_prop);

# Power
vendor_internal_prop(power_prop);

# Sensors
vendor_internal_prop(sensors_panel_prop);

# Spectrum
vendor_public_prop(spectrum_prop);

# Thermal engine
vendor_internal_prop(thermal_prop);

# Dirac
vendor_public_prop(dirac_prop);

# WiFi
vendor_internal_prop(wifihal_prop);
