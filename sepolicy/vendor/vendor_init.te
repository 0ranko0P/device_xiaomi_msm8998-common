typeattribute vendor_init data_between_core_and_vendor_violators;

allow vendor_init {
    system_data_file
    thermal_data_file
}:dir create_dir_perms;

get_prop(vendor_init, spectrum_prop)

set_prop(vendor_init, camera_prop)
set_prop(vendor_init, power_prop)
